
#wizard-status-bar
    h1 Your order
    .order-table

form#stripe-form(method='post', action='.', style="margin-top: 0.8em")

    fieldset(style="overflow: hidden; margin-bottom: 0.6em")

        label.card-number Credit Card Number
            input#billing-card-number.unknown(type='tel', placeholder='Card Number', required='required', maxlength='18')

        label.expiry-date Expiry Date
            input#billing-expiry(type='tel',  placeholder="MM / YY", maxlength='7', required='required')

        label.cvc CVC
            input#billing-cvc(type='tel', placeholder='123', maxlength='4', required='required')

    button#wizard-next-step.btn-big(type="submit", disabled=true) Fill in all fields
