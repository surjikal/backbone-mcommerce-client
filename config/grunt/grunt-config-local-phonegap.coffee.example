
module.exports =

    # Where to put the result of the grunt task (relative to project root)
    build_directory: 'phonegap/iOS/www'

    # These files will be deleted at the end of the grunt task
    tmp_directory:   'tmp'

    # Shows the baseline, helpful for css development
    show_baseline: false

    # Weinre remote debugger configuration
    remote_debugging:

        # Should we include code for remote debugging?
        enabled: false

        # The location of the remote debugging script
        script_url: 'http://debug.phonegap.com/target/target-script-min.js#vipl-phonegap-debug'

        # The url used to access the remote debugging interface
        interface_url: 'http://debug.phonegap.com/client/#vipl-phonegap-debug'
